{
  "name": "vasi",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev:web": "pnpm --filter vasi-web dev",
    "dev:api": "pnpm --filter vasi-api dev",
    "build:web": "pnpm --filter vasi-web build",
    "build:api": "pnpm --filter vasi-api build",
    "lint": "pnpm -r lint",
    "db:migrate:local": "pnpm --filter vasi-api exec wrangler d1 migrations apply vasi-db --local",
    "db:migrate:prod": "pnpm --filter vasi-api exec wrangler d1 migrations apply vasi-db"
  },
  "workspaces": [
    "vasi-web",
    "vasi-api"
  ]
}
